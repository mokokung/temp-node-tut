// Module
const names = require('./4.name')
const SayHi = require('./5.util')
const Uptome = require('./6.alternative-flavor')
require('./7.mind-grenade')

SayHi('susan')
SayHi(names.john) //เรียก function มาใช้และเอาค่าจาก
SayHi(names.peter)
